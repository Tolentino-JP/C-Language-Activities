//For loop:
//multiplication table

#include <stdio.h>

int main() {
    int n, i, e;
    
    printf("Enter an integer: "); 
    scanf("%d", &n);
    
    for(i=1;i<11;i++){
        e = n * i;
        printf("%d x %d = %d\n", n, i, e);
    }
    return 0;
}
